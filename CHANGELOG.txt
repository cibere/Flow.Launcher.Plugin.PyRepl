# v0.0.3

- If no code is provided, use clipboard content
- Add the ability to add custom ENV values through a dictionary in json format
- ensure traceback window is at the top when created
- Add "An error has occured" result, which shows the tb when clicked
    - Added a `Just show me the traceback` settings option to keep the pre-0.0.3 behavior of just showing the traceback.